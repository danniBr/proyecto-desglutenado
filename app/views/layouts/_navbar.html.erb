<nav class="navbar navbar-expand-lg">
  <a class="navbar-brand" href="<%= root_path %>"><%= image_tag 'logo.png' %></a>
  <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
    <span class="navbar-toggler-icon"><i class="fa fa-navicon"></i></span>
  </button>

  <div class="collapse navbar-collapse justify-content-between" id="navbarSupportedContent">
    <ul class="navbar-nav ml-auto">
      <li class="nav-item"><%= link_to 'Recetas', recipes_path, :class => 'btn color1' %></li>
      <li class="nav-item"><%= link_to 'Preguntas', questions_path, :class => 'btn color2' %></li>
      <li class="nav-item"><%= link_to 'Dónde comprar', locations_path, :class => 'btn color3' %></li>
      <li class="nav-item">
        <button class="btn btn-user" type="button" data-toggle="collapse" data-target="#collapseExample" aria-expanded="false" aria-controls="collapseExample">
          <i class="fa fa-user-circle"></i>
        </button>
      </li>
    </ul>
    <div class="collapse" id="collapseExample">
     <div class="desplegable">
       <% if user_signed_in? %>
       <a class="nav-link" href="<%= edit_user_registration_path%>">Editar usuario</a>
         <a class="nav-link" href="<%= destroy_user_session_path%>" data-method="DELETE">Salir</a>
       <% else %>
         <a class="nav-link" href="<%= new_user_session_path%>">Iniciar sesión</a>
         <a class="nav-link" href="<%= new_user_registration_path%>">Registrarse</a>
       <% end %>
     </div>
   </div>
  </div>
</nav>
