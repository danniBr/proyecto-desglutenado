<nav class="navbar navbar-default">
  <div class="container-fluid">
    <div class="navbar-header">
      <button type="button" class="navbar-toggle collapsed" data-toggle="collapse" data-target="#bs-example-navbar-collapse-1" aria-expanded="false">
        <span class="sr-only">Toggle navigation</span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
      </button>
      <a class="navbar-brand" href="<%= root_path %>">Desglutenado</a>
    </div>
    <!-- Collect the nav links, forms, and other content for toggling -->
    <div class="collapse navbar-collapse" id="bs-example-navbar-collapse-1">
      <ul class="nav navbar-nav navbar-right">
		  <% if user_signed_in? %>
		    <li><a href="<%= destroy_user_session_path%>" data-method="DELETE">Salir</a></li>
      <% else %>
        <li><a href="<%= new_user_session_path%>">Iniciar sesión</a></li>
        <li><a href="<%= new_user_registration_path%>">Registrarse</a></li>
		  <% end %>
      </ul>
    </div><!-- /.navbar-collapse -->
  </div><!-- /.container-fluid -->
</nav>

<nav><ul>
	
</ul>
</nav>