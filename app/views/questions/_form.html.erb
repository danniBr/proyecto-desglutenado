<%= form_with(model: question, local: true) do |form| %>
  <% if question.errors.any? %>
    <div id="error_explanation">
      <p><%= pluralize(question.errors.count, "error") %> No se ha podido guardar la pregunta:</p>

      <ul>
      <% question.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :Título %>
    <%= form.text_field :title, id: :question_title %>
  </div>

  <div class="field">
    <%= form.label :Pregunta %>
    <%= form.text_area :description, id: :question_description %>
  </div>

  <div class="actions">
    <%= form.submit "Ingresar", :class => 'btn' %>
  </div>
<% end %>
